If-Statement Pitfalls
=====================

.. todo::

   Common Python mistakes: using ``=`` instead of ``==``, off-by-one in
   chained conditions, missing ``elif`` (leading to redundant checks).
   Python-specific: ``if x:`` vs ``if x is not None:`` vs ``if x != 0:``.
