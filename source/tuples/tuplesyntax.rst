Tuple Syntax
============

.. todo::

   Python tuples: ``(1, 2, 3)``, immutability vs. lists, single-element tuple
   ``(x,)``. When to use tuples vs. lists. No C# equivalent — this is
   Python-specific.
