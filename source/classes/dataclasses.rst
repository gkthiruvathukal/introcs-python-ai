Python Dataclasses
==================

.. todo::

   Introduce ``@dataclass`` as a modern, concise way to define classes that
   primarily hold data. Replaces C#'s struct/record coverage. Show
   automatic ``__init__``, ``__repr__``, and ``__eq__`` generation.
