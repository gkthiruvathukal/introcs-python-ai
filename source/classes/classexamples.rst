Class Instance Examples
=======================

.. todo::

   Python equivalents of averager, clock examples. Show instance state,
   ``__str__`` for string representation (replaces C#'s ``Print()`` method),
   and ``__repr__``.
