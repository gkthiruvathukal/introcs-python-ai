List Syntax
===========

.. todo::

   Python list literals ``[1, 2, 3]``, indexing, slicing, ``len()``,
   ``in`` operator. Lists are mutable (unlike strings). Mixed-type lists possible.
   Covers both C#'s array and List<T> use cases.
