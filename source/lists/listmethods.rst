List Methods
============

.. todo::

   ``.append()``, ``.extend()``, ``.insert()``, ``.remove()``, ``.pop()``,
   ``.sort()``, ``.reverse()``, ``.index()``, ``.count()``.
   Built-in functions: ``sorted()``, ``reversed()``, ``min()``, ``max()``, ``sum()``.
