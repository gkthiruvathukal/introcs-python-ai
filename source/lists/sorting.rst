Sorting
=======

.. todo::

   Sorting algorithms: bubble sort, insertion sort, selection sort — Python
   equivalents of the C# sorting examples. Also cover Python's built-in
   ``list.sort()`` and ``sorted()`` with ``key=`` and ``reverse=`` parameters.
