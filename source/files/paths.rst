Path Strings
============

.. todo::

   File paths in Python. Introduce ``pathlib.Path`` as the modern approach.
   Also cover the older ``os.path`` for compatibility. Cross-platform path
   handling (``/`` vs ``\``).
