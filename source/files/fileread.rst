Reading Files
=============

.. todo::

   ``open()`` and the ``with`` statement. Reading line by line with ``for line in f:``.
   ``f.read()``, ``f.readlines()``. Python equivalents of print_file_lines, sum_file.
