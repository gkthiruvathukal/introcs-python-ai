Working with ``pathlib``
========================

.. todo::

   Modern Python file system operations with ``pathlib.Path``:
   ``Path.iterdir()``, ``Path.glob()``, ``Path.exists()``,
   ``Path.stem``, ``Path.suffix``. Replaces C#'s ``Directory`` class coverage.
