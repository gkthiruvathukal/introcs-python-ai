Combining Input and Output
==========================

.. todo::

   Show the ``input()`` function: always returns a string, use ``int()``
   or ``float()`` to convert. Contrast with C#'s ``Console.ReadLine()``
   + ``double.Parse()``. Show prompt-in-input pattern: ``input("Enter x: ")``.
