Writing to the Screen
=====================

.. todo::

   Cover the ``print()`` function: multiple arguments, ``sep`` and ``end``
   parameters. Compare ``print()`` vs ``print(..., end='')`` to C#'s
   ``Console.WriteLine`` vs ``Console.Write``.
