Types and Conversions
=====================

.. todo::

   Cover Python's built-in types: ``int``, ``float``, ``str``, ``bool``.
   Type conversion functions: ``int()``, ``float()``, ``str()``, ``bool()``.
   The ``type()`` function. Note: no explicit declarations needed — types are
   inferred from values.
