Arithmetic
==========

.. todo::

   Cover integer and float arithmetic in Python.
   Use the Python REPL (``python3``) to demonstrate expressions interactively —
   a key advantage over C# where a scratch project was required.

   Topics: ``+``, ``-``, ``*``, ``/``, ``//`` (floor division), ``%``, ``**``.
   Note integer vs. float division behavior.
