F-Strings and String Formatting
================================

.. todo::

   Cover f-strings (``f"..."``): embedding expressions, format specifiers
   (``{value:.2f}``, ``{value:>10}``). Compare to C#'s
   ``Console.WriteLine("{0:F2}", value)`` format strings.
   Also mention ``str.format()`` for completeness.
