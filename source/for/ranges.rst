The ``range()`` Function
========================

.. todo::

   ``range(n)``, ``range(start, stop)``, ``range(start, stop, step)``.
   Equivalents of C# ``for (int i = 0; i < n; i++)``.
   Reverse iteration with negative step.
