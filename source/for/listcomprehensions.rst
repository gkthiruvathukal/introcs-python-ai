List Comprehensions
===================

.. todo::

   Introduce list comprehensions as a concise alternative to ``for`` loops
   that build lists. ``[expr for item in iterable]`` and
   ``[expr for item in iterable if condition]``. Python-specific — no C# equivalent.
