For Loop Syntax
===============

.. todo::

   Python's ``for`` loop is a foreach: ``for item in iterable:``.
   This single construct covers both C#'s ``foreach`` and ``for`` loop use cases.
   Show iteration over strings, lists, and ``range()``.
