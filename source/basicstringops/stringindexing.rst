String Indexing
===============

.. todo::

   Python string indexing: ``s[0]``, ``s[-1]``, slicing ``s[1:4]``,
   ``s[:3]``, ``s[2:]``. Note 0-based indexing and negative indices
   (a Python feature not in C#).
