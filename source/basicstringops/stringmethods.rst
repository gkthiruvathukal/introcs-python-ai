String Methods and Length
=========================

.. todo::

   Cover commonly used string methods: ``len()``, ``.upper()``, ``.lower()``,
   ``.strip()``, ``.replace()``, ``.split()``, ``.join()``, ``.find()``,
   ``.startswith()``, ``.endswith()``. Note immutability.
