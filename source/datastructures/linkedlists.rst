Linked Lists
============

.. todo::

   Implementing a singly linked list in Python using a ``Node`` class.
   Insertion, traversal, deletion. Python-specific: using ``None`` as the
   null pointer equivalent.
