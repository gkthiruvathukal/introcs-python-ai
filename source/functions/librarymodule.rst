Library Modules
===============

.. todo::

   Replace C#'s "Library Classes" with Python's module system.
   Cover ``import math``, ``math.sqrt()``, ``math.pi``, ``random`` module.
   Contrast with C#'s static library classes.
