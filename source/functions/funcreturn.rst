Returned Function Values
========================

.. todo::

   Python equivalent of return1/return2/addition2. ``return`` statement,
   using the returned value in an expression. Note: no ``void`` equivalent —
   functions that don't return a value implicitly return ``None``.
