Local Scope
===========

.. todo::

   Python equivalent of bad_scope/good_scope. Variables defined inside a
   function are local. Note: Python's scoping rules (LEGB) differ slightly
   from C# — introduce just the local/global distinction here.
