Dictionary Examples
===================

.. todo::

   Python equivalent of count_words (word frequency on Gettysburg Address).
   Show ``.items()``, ``.keys()``, ``.values()`` iteration.
   Introduce ``collections.Counter`` as a modern alternative.
