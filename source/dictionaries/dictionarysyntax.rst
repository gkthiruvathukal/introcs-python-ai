Dictionary Syntax
=================

.. todo::

   Python dict literals ``{key: value}``, accessing values ``d[key]``,
   ``d.get(key, default)``, adding/updating entries, ``del d[key]``,
   ``key in d``. Note: Python dicts preserve insertion order (3.7+).
