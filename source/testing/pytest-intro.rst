Introduction to pytest
======================

.. todo::

   Python testing with ``pytest``. Writing test functions (``def test_*``),
   using ``assert``. No test class required for simple cases.
   Running tests with ``pytest`` from the command line.
   Contrast with C#'s NUnit ``[Test]`` attribute approach.
